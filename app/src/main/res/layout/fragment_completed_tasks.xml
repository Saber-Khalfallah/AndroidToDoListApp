<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".CompletedTasksFragment">

    <!-- RecyclerView to display the completed tasks -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewCompletedTasks"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp" />

    <!-- TextView to display when there are no completed tasks -->
    <TextView
        android:id="@+id/tvEmptyCompletedTasks"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="No completed tasks available."
        android:textSize="16sp"
        android:textColor="@android:color/darker_gray"
        android:visibility="gone"
        android:layout_gravity="center" />
</FrameLayout>
